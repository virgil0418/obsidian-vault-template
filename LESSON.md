# Lessons — Obsidian Vault Template

> 从 agent 维护、知识库整理和 review 过程中总结的可复用经验

## 0. 先读 AGENTS，再动知识库

**场景**：agent 需要整理、修改或回答基于本仓库的问题。

**反模式**：直接全文搜索或按通用 Obsidian 习惯改文件，忽略本仓库的目录语义和来源保护规则。

**正确做法**：先读 `AGENTS.md`，确认当前任务属于 ingest、query、promote、lint 还是 maintain，再决定读写路径。

**Lesson**：`AGENTS.md` 是本仓库最高操作 schema；任何维护动作都应先对齐它的目录语义、页面类型和工作流。

---

## 1. 工作项进 WORK，稳定知识进 Atlas/Notes

**场景**：一次 agent 任务产生了拆解步骤、阻塞点、验证结果，也可能沉淀出通用知识。

**反模式**：把执行 checklist 写进 `Atlas/Notes`，或把稳定知识只留在聊天和工作记录中。

**正确做法**：执行状态、待办和验证证据写入 `WORK.md`；脱离当前任务后仍可复用的概念、方法和结论，晋升到 `Atlas/Notes` 并建立来源链接。

**Lesson**：工作记录和知识沉淀要分层；`WORK.md` 负责“这件事怎么推进”，`Atlas/Notes` 负责“以后还值得复用什么”。

---

## 2. 来源正文默认只读

**场景**：`Atlas/Sources` 中的网页剪藏、书摘、课程笔记、语音转文字或 PDF 转 Markdown 包含有用内容。

**反模式**：为了让来源“更像 wiki 页面”，重写来源正文、改写结构，或把总结直接覆盖到来源文件里。

**正确做法**：保持来源近原始状态；只在必要时补充少量 frontmatter。可复用结论应提炼到 `Atlas/Notes`，并通过 `sources` 或正文链接回来源。

**Lesson**：来源材料是证据层，不是综合稿草稿区；保护来源能让后续核查和再解释保持可靠。

---

## 3. x 是控制面，不是知识层

**场景**：需要调整模板、按钮、附件、隐藏模板、CSS snippets 或其他支持资产。

**反模式**：把 `x` 当作临时收件箱或知识卡目录，导致控制资产和知识内容混在一起。

**正确做法**：把 `x` 作为 schema / control plane 层，只放让知识库创建、呈现、维护得以运行的控制资产。稳定知识仍放 `Atlas/Notes`，导航结构仍放 `Atlas/Maps`。

**Lesson**：控制面要服务知识库运行，但不承载 canonical 知识；这样 agent 才能稳定地区分“内容”和“控制规则”。

---

## 4. 维护日志只记录有意义的结构变化

**场景**：agent 修改了 schema、地图、Base、模板或进行了重要 ingest / promote / lint。

**反模式**：每个小改动都写日志，导致 `Atlas/log.md` 噪声过高；或重大结构变更不写日志，导致后续 agent 无法回溯。

**正确做法**：只有当工作实质改变仓库结构、导航、schema 或重要内容时，才按 `AGENTS.md` 的标题格式追加 `Atlas/log.md`。

**Lesson**：`Atlas/log.md` 是机器友好的维护时间线；它应该短、准、可回溯，而不是流水账。

---

## 5. 可复用经验要写成可迁移的判断规则

**场景**：某次任务中发现了一个经验，例如“先验证 API 再写代码”或“来源材料不要重写”。

**反模式**：只记录当时发生了什么，缺少适用场景、反模式和可执行做法。

**正确做法**：每条 lesson 至少写清楚场景、反模式、正确做法和一句可迁移的 Lesson。

**Lesson**：好的 lesson 不是复盘情绪，而是未来 agent 可以直接应用的判断规则。
