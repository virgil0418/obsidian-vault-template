# WORK — Obsidian Vault Template

> Agent 拆解的工作项、进度、问题与决策记录

## 当前状态

- **最后更新**：2026-04-27
- **仓库角色**：人类与 agent 共同维护的 Obsidian 知识库模板
- **Schema**：`AGENTS.md`
- **控制面**：`x`
- **当前重点**：建立 agent 工作拆解与 lesson 记录入口
- **状态**：active

## 默认工作流约定

### Agent 拆解工作项的默认语义

`WORK.md` 用来记录 agent 对工作的拆解、执行状态、阻塞点、验证结果和关键决策。

默认含义是：

1. 先把需求拆成可验证的工作项，而不是直接散落在聊天里。
2. 每个工作项应有明确的输出位置、完成标准和验证方式。
3. 执行中发现的稳定经验，沉淀到 `LESSON.md`。
4. 如果工作实质改变了知识库结构、schema 或重要内容，同步追加 `Atlas/log.md`。
5. 如果工作产出了稳定可复用知识，再按 `AGENTS.md` 晋升到 `Atlas/Notes` 或 `Atlas/Maps`。

这里的“工作项”专指 agent 为完成一次维护、整理、查询、晋升、lint 或实现任务而拆出的可追踪任务；不替代 `Efforts` 中的人类项目管理，也不替代 `Calendar` 中的时间记录。

### 为什么这样约定

- 聊天记录不稳定，难以被后续 agent 接续。
- `Atlas/log.md` 适合记录维护时间线，但不适合承载进行中的任务拆解。
- `Efforts` 面向人的行动和交付，不应被 agent 的临时执行细节污染。
- 独立的 `WORK.md` 可以让 agent 在跨会话工作时快速恢复上下文。

### 执行边界

- 不要把每一次微小编辑都写进 `WORK.md`；只记录有拆解价值或需要交接的工作。
- 不要把 `WORK.md` 当作 canonical 知识层；稳定知识应进入 `Atlas/Notes`。
- 不要把 `LESSON.md` 当作问题清单；它只记录已经被验证或反复出现的经验。
- 如果用户明确要求只做一次性修改，可以只更新文件，不强制追加工作项。

### Success criteria

一个可交接的工作项至少应能回答：

- 要解决什么问题？
- 涉及哪些文件或目录？
- 当前状态是什么？
- 完成标准是什么？
- 用什么命令、检查或人工标准验证？
- 如果完成了，结果和证据是什么？

## 进行中

- [ ] **建立 agent 工作记录习惯（2026-04-27）**：后续有多步骤维护任务时，先在本文件记录拆解、状态和验证结果。

## 已完成（近期）

- [x] **建立 WORK/LESSON 维护入口（2026-04-27）**：新增 `WORK.md` 记录 agent 拆解工作项，新增 `LESSON.md` 记录可复用经验，并在 `AGENTS.md` 与知识库总图中补充入口。

## 已完成

- [x] **建立 agent 维护型知识库 schema（2026-04-26）**：新增 `AGENTS.md`，明确 `Atlas/Sources`、`Atlas/Notes`、`Atlas/Maps`、`Atlas/Bases`、`+`、`Calendar`、`Efforts` 与 `x` 的职责。
- [x] **将 x 明确为控制面层（2026-04-27）**：将 `x` 从普通基础设施升级为 schema / control plane 层，承载模板、按钮、附件、样式等控制资产。

## 待办

- [ ] 后续执行较复杂的 ingest / promote / lint 时，把任务拆解追加到“进行中”。
- [ ] 当某个工作项完成后，将其移动到“已完成（近期）”，并保留验证证据。
- [ ] 如果某条经验可复用，提炼到 `LESSON.md`，避免只留在工作记录里。

## 遇到的问题

- 暂无。

## 决策记录

- **2026-04-27**：`WORK.md` 放在仓库根目录，作为 agent 执行控制文档；它不属于稳定知识层，不替代 `Atlas/Notes`。
- **2026-04-27**：`LESSON.md` 放在仓库根目录，作为 agent 经验沉淀文档；它记录工作方法，不替代来源材料或知识卡。
